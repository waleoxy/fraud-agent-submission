{
  "name": "fraud-agent-core",
  "version": "0.3.2",
  "description": "ERP-agnostic autonomous fraud investigation agent (Gemini 3.5 + Genkit). Implement one adapter interface to plug into any ERP.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "lint": "tsc --noEmit"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0 || ^11.0.0 || ^12.0.0",
    "reflect-metadata": "^0.2.0"
  },
  "dependencies": {
    "genkit": "^1.0.0",
    "@genkit-ai/google-genai": "^1.0.0",
    "@google-cloud/firestore": "^7.10.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  },
  "keywords": ["fraud-detection", "agent", "gemini", "genkit", "nestjs", "erp"],
  "license": "UNLICENSED",
  "private": false
}
